import { ScrollpageDirective } from './scrollpage.directive';

describe('ScrollpageDirective', () => {
  it('should create an instance', () => {
    const directive = new ScrollpageDirective();
    expect(directive).toBeTruthy();
  });
});
